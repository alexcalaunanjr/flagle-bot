this app is a telegram bot that does flagle, wordle but for flags, similar to https://flagle-game.com/. I'd like to use FastAPI for the backend, use python telegram library for it. im not sure if we should be storing flag images in a database/bucket or from an existing API. im hoping this bot will have daily flagle and random flagle as well. apart from the backend, what is the architecture for this app considering there's going to be a need for database of flags, then also potentially a sort of storage for the flags (object storage, etc)? preferably the stack should be minimal cost, including hosting the bot

Users have 6 tries to guess a flag. After each guess, pixels of the solution that overlap with the guessed flag will be revealed. The flags have been scaled to the same aspect ratio. if you want more information on flagle game, only reference https://flagle-game.com/
